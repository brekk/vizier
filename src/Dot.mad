enquote = (x) => `"` ++ x ++ `"`

export attributeWithDelimiter = (d, literal, k, v) => `${k} = ${!literal ? enquote(v) : v}${d}`
export attribute = attributeWithDelimiter("")
export attr = attribute(false)
export literalAttr = attribute(true)
